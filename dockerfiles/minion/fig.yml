---

minion:
  build: .
  volumes:
    - /:/host
  privileged: true
  ports:
    - "7946"
  environment:
    LOCAL_REGISTRY: 192.168.59.103:5000
    DOCKER_BINARY: /usr/bin/docker
    DOCKER_SOCKET: /run/docker.sock
    CONTACT:
    ROLE: minion